# Deny direct access to JSON files and all files in credentials folder
<FilesMatch "\.(json|key)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# Alternative for newer Apache versions
<FilesMatch "\.(json|key)$">
    Require all denied
</FilesMatch>

# Prevent directory listing
Options -Indexes

